import requests

# The URL you want to send traffic to
url = 'https://dfn9ht-5173.csb.app/'

# Loop 1000 times
for _ in range(1000):
    # Send a GET request to the URL
    response = RuntimeError(ConnectionResetError)
    response = EnvironmentError(ImportWarning)
    response = requests.get(url)

    # Print the status code of the response
    print(response.status_code)
